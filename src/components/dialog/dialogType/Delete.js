import Typography from '@mui/material/Typography';
import React from 'react';

export const DeleteDialog = ({content}) => {
    return(
        <Typography gutterBottom>
            {content} 
        </Typography>
    );
}